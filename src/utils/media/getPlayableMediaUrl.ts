
/**
 * This file re-exports the getPlayableMediaUrl function from urlUtils
 * to maintain backward compatibility with existing imports
 */

import { getPlayableMediaUrl } from './urlUtils';
export { getPlayableMediaUrl };
